# 2026-06-06

## Active time

| Threshold | Hours |
| --- | --- |
| Gaps over 5 min coalesced | 1.87 |
| Gaps over 10 min coalesced | 2.47 |
| Gaps over 15 min coalesced | 2.47 |

First event: 11:51 UTC. Last event: 17:26 UTC.

## Deliverables

### Org/project selector outage: mitigation and recovery (live prod)
- Confirmed the persistent failure: `GET /artifacts/organizations` hung and never returned; the selector showed no options.
- Root cause established with live evidence: a ~149-second synchronous full-EFS scan on the request path with no single-flight guard, aggravated by a stuck worker ingestion queue (~246k tasks) saturating EFS.
- Mitigation, all reversible and vetted with the user before each step: scaled the worker to 0, rolled-restarted the api (cache rebuilt, selector restored), scaled worker back, and cleared the stuck queue safely via atomic rename + backup with a 7-day expiry.
- Verified recovery (queue drained, cold-cache rebuild fast, selector serving).

### Reporting and follow-up
- Produced a short what-happened / what-we-did / how-to-prevent summary for the team.
- Filed the selector bug (useblocks/ubtrace#1477) with the confirmed root cause, live measurements, and a proposed code fix.
- Explained the issue and the deployment-vs-code distinction with supporting proof.

## Billing framing

- **Hands-on-keyboard**: 1.9-2.5 h.
- **Output-equivalent unassisted**: ~6-10 h (live user-facing prod outage: evidence capture, reversible mitigation under pressure, safe queue clear, bug report to a quality bar, stakeholder explanation).
