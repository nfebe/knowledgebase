# 2026-05-22

## Active time

| Threshold | Hours |
| --- | --- |
| Gaps over 5 min coalesced | 2.31 |
| Gaps over 10 min coalesced | 2.31 |
| Gaps over 15 min coalesced | 2.94 |

First event: 07:28 UTC. Last event: 17:07 UTC.

## Deliverables

### Prod deploy: ubTrace v1.4.0-dev.38
- Helm release `ubtrace` rev 18 -> 19 on `ubtrace-prod`. End-to-end via `deploy.sh apply`, smoke test passed on all four hosts.

### PR work

- Extended [ubinfra#156](https://github.com/useblocks/ubinfra/pull/156) with the EKS Terraform stack (later trimmed), then with `deploy.sh`, the dry-run CI workflow, a tunnel subcommand, and the prod values overlay bumped to dev.38.
- Extended [ubtrace#1402](https://github.com/useblocks/ubtrace/pull/1402) with chart Helm additions and the publish workflow draft for chart-to-OCI + Terraform-as-release-asset.
- Created `ubinfra/.github/workflows/ubtrace-prod-deploy.yml` (manual deploy via GitHub OIDC).
- Created `ubinfra/ubtrace/repo-setup.md` documenting AWS OIDC trust policy + repo secrets/variables/environments.

## Billing framing

- **Hands-on-keyboard**: 2.3-2.9 h.
- **Output-equivalent unassisted**: ~6-10 h (prod deploy + two PRs extended across two repos + CI workflows + documentation).
