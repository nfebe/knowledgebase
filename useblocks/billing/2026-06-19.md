# 2026-06-19

## Active time

| Threshold | Hours |
| --- | --- |
| Gaps over 5 min coalesced | 4.19 |
| Gaps over 10 min coalesced | 4.93 |
| Gaps over 15 min coalesced | 5.13 |

First event: 00:23 UTC. Last event: 20:59 UTC.

## Deliverables

### Decouple security hardening from stack identifiers (ubinfra #182)
- Reworked the security-group and network-ACL hardening to discover their targets at run time by the `stack=runs-on` tag instead of pinning resource IDs in config, so the hardening follows the stack across any recreation (which the v2 to v3 cutover forces).
- Validated against the live stack: discovery resolved the current resources and the plan was a no-op for both modules. Merged.

### Stand up RunsOn v3 as Terraform (ubinfra #183)
- Authored a new Terraform stack that replaces the console-created v2 runner stack with reproducible infrastructure: the Flex (webhook-driven) model to match v2 so existing `runs-on:` labels keep working, a dedicated parallel VPC tagged for the new stack, native 365-day log retention, and the module pinned to a fixed version.
- Validated with format, init, validate, and a dry plan (127 to add, none changed or destroyed). The apply was intentionally not run from CI: it needs admin credentials in the CI account plus the license key, alert email, and deploy-role secrets. Merged.

### Cut over hardening and AMI rebuild to the live v3 stack (ubinfra #184)
- After v3 went live serving CI, repointed the security automation and the AMI rebuild subnet lookup to the v3 stack, closing the window where v3 ran production CI on unhardened defaults (public SSH open) because the hardening was still discovering v2.
- Granted the CI role the network-ACL permissions its hardening run against v3 needed. Merged.

## Billing framing

- **Hands-on-keyboard**: ~4.2-5.1 h.
- **Output-equivalent unassisted**: ~12-16 h (three Terraform pull requests authored, validated, and merged in sequence: a hardening rework to track resources by tag, a full runner control plane rebuilt as infrastructure-as-code with a parallel VPC, and a live cutover with the security automation repointed, all under production CI constraints).
