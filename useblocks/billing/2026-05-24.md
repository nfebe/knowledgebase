# 2026-05-24

## Active time

| Threshold | Hours |
| --- | --- |
| Gaps over 5 min coalesced | 0.52 |
| Gaps over 10 min coalesced | 0.52 |
| Gaps over 15 min coalesced | 0.52 |

First event: 09:54 UTC. Last event: 10:25 UTC.

## Deliverables

### Code review responses (Marco's reviews on both PRs)

- [ubinfra#156](https://github.com/useblocks/ubinfra/pull/156): fixed the Keycloak RDS endpoint typo in `prod-rds-access.md`; added `require_known_context` guard to `deploy.sh diff-live`.
- [ubtrace#1402](https://github.com/useblocks/ubtrace/pull/1402): flipped chart `runAsGroup` / `fsGroup` defaults from `0` to `1001` (OpenShift compatibility moved to overlay); tightened `eks_endpoint_public_access_cidrs` module default to `[]` with validation; reverted publish workflow `runs-on` to `ubuntu-latest` for release-time decoupling.

## Billing framing

- **Hands-on-keyboard**: 0.5 h.
