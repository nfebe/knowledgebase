# 2026-05-20

## Active time

| Threshold | Hours |
| --- | --- |
| Gaps over 5 min coalesced | 3.63 |
| Gaps over 10 min coalesced | 4.57 |
| Gaps over 15 min coalesced | 4.75 |

First event: 10:22 UTC. Last event: 20:53 UTC.

## Deliverables

### Prod deploy: ubTrace v1.4.0-dev.35
- Helm release `ubtrace` rev 17 -> 18 on `ubtrace-prod` (account 809338888439).
- RDS pre-snapshots: `ubtrace-prod-app-pre-dev35-2026-05-20`, `ubtrace-prod-keycloak-pre-dev35-2026-05-20`.
- Post-deploy smoke test passed on all four hosts + API readiness.

### PRs landed (open at end of session)
- [ubinfra#156](https://github.com/useblocks/ubinfra/pull/156): prod runbooks, dev cluster instance config, EKS Terraform stack, prod values overlay, helm upgrade runbook.
- [ubtrace#1402](https://github.com/useblocks/ubtrace/pull/1402): Keycloak config-cli Helm templates for Google IdP and email policy.

### Investigations
- "Ingestion every minute" on prod: root-caused to `ubt-sphinx 0.8.0` wheel shipping without the Sphinx `ubtrace` builder entry-point metadata; one failing project (`useblocks/okr/v1`) cycles every 60 s. Fix lives in the `useblocks/ubt-sphinx` build pipeline. Issue [useblocks/ubt-sphinx#51](https://github.com/useblocks/ubt-sphinx/issues/51) opened.
- ubinfra Dependabot alert regression: traced 21 of 26 open alerts to PR #149 (May 15, restored `sst@2.49.6` + `aws-cdk-lib@2.201.0` + `dotenv` in `deploy/sst/package.json`).

## Billing framing

- **Hands-on-keyboard**: 4.5-4.8 h.
- **Output-equivalent for an unassisted senior engineer**: 12-18 h (two prod deploys with documentation, three-PR migration across two repos, three runbooks, two root-cause investigations).
