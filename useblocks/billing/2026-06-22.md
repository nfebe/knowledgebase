# 2026-06-22

## Active time

| Threshold | Hours |
| --- | --- |
| Gaps over 5 min coalesced | 1.96 |
| Gaps over 10 min coalesced | 1.96 |
| Gaps over 15 min coalesced | 2.67 |

First event: 00:03 UTC. Last event: 21:05 UTC.

## Deliverables

### ubTrace 1.6.0-rc.0 prod deploy (team.useblocks.com)
- Verified the reconciled prod Terraform state was clean (no infra apply needed), opened the SSM tunnel, took pre-deploy RDS snapshots of both prod databases, ran the atomic helm upgrade to 1.6.0-rc.0, and confirmed the smoke test green (all hosts plus every readiness dependency up).
- Documented the current prod deploy process in the knowledge base and corrected the stale "prod has no terraform state" record.

### EKS PR review fixes (ubtrace #1402, ubinfra #156)
- Verified both PRs against the live branch state, then implemented the outstanding review items: scoped the chart's group-0 securityContext to only the bundled stateful images (stateless pods now run non-root group 1001); moved the prod Terraform instance config out of the public template repo into ubinfra and excluded it from the published artifact; and hardened the deploy automation (raised the helm timeout above the chart startup budget, guarded the dry-run manifest extraction, switched the image-tag check to a YAML parse, de-duplicated the certificate ARN, and separated the dev-cluster archive from the prod runbooks).
- Replied per point on both reviews and opened two tracking issues for the deferred items (the cold-boot catalogue walk and the dry-run chart pinning).

## Billing framing

- **Hands-on-keyboard**: ~2.0-2.7 h.
- **Output-equivalent unassisted**: ~10-14 h (a live production release with snapshots and verification, plus a multi-part response to two infrastructure PR reviews spanning a Helm chart, Terraform, and CI automation).
