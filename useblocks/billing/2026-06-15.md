# 2026-06-15

## Active time

| Threshold | Hours |
| --- | --- |
| Gaps over 5 min coalesced | 2.77 |
| Gaps over 10 min coalesced | 3.18 |
| Gaps over 15 min coalesced | 3.41 |

First event: 07:59 UTC. Last event: 18:54 UTC.

## Deliverables

### Status monitor: stale-run hardening (ubinfra #171)
- Closed the gap the dead man's switch missed: the Lambda now emits a success heartbeat only after a fully completed run, and a new alarm fires when no successful run completes for 15 minutes, so a Lambda that is invoked but crash-looping is treated as down.
- Deployed to prod, verified the heartbeat metric is flowing and all alarms sit in OK. Opened PR #176 (engine) and PR #178 (stale-run, stacked).

### Terraform state recovery and production reconciliation (ubtrace #1402 branch)
- Established that the prod Terraform state was never lost: the committed backend config pointed at a defunct bucket name; the real state lives in the prod account and reads fine. Corrected the backend config.
- With state reachable, a plan would have destroyed and rebuilt almost the entire prod stack. Traced it to a single environment-name mismatch (the branch renamed every resource) and reconciled the config to the running cluster, taking the plan from 76 destroys to a clean no-op.
- Sized the database and cache config down to the modest tier actually running (the HA spec is recorded as a deferred decision), matched the live multi-domain TLS certificate so Terraform keeps rather than deletes it, added a guardrail so the live certificate can never be removed by a config change, and stopped the bastion being replaced on every base-image release.
- Committed and pushed the reconciliation; rebased the branch onto its base.

### EFS throughput switch and selector recalibration (ubtrace #1528)
- Switched the production EFS to elastic throughput through Terraform, removing the burst-credit exhaustion that throttled it.
- Re-measured the catalogue walk on elastic (11s cold, 6s warm across 6,354 directories) and showed the 149s baseline was throttling-inflated, not intrinsic. Confirmed api and worker run a single replica each.
- Posted the findings and recalibration on the PRD and the P1/P4 tickets, closed out #1528, and drafted the team channel note.

### Knowledgebase and billing
- Documented the status-monitoring work and the Terraform reconciliation in the knowledgebase; logged billable hours.

## Billing framing

- **Hands-on-keyboard**: 2.8-3.4 h.
- **Output-equivalent unassisted**: ~10-14 h (a production Terraform state recovery and full config reconciliation under live constraints, a certificate and storage change applied safely, a monitoring hardening with deploy and verification, an ops recalibration with measurements, and two pull requests).
