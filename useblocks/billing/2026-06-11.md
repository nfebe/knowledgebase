# 2026-06-11

## Active time

| Threshold | Hours |
| --- | --- |
| Gaps over 5 min coalesced | 0.91 |
| Gaps over 10 min coalesced | 1.14 |
| Gaps over 15 min coalesced | 1.71 |

First event: 00:44 UTC. Last event: 22:19 UTC.

## Deliverables

### ubTrace PROD status monitoring: design and build (ubinfra #170)
- Designed the monitoring approach: studied the existing `download_heartbeat` stack as the template, settled the two-layer model (alerting engine now, public status page later), and confirmed it belongs in ubinfra with no new repo.
- Built the reachability engine: a new Terraform stack (EventBridge schedule, Lambda, SNS, CloudWatch alarms) checking the ubTrace public surface against per-target expected codes (including the api-root 404 and auth 302), with a two-strike alarm/reminder/recovery state machine and an offline self-test.

### Review and fixes
- Ran an independent two-agent review; fixed two real findings before deploy: an observability alarm that watched a metric with no data, and a state-machine crash path on malformed stored state.

### Deploy and live verification (prod)
- Deployed to ubtrace-prod (state bucket bootstrap + stack apply), then verified live: all five targets read correctly, the schedule runs autonomously every 5 minutes, and a forced-failure test alarmed after exactly two misses and recovered.

## Billing framing

- **Hands-on-keyboard**: 0.9-1.7 h.
- **Output-equivalent unassisted**: ~6-9 h (design from a reference stack, build a monitored Terraform + Lambda stack, independent review with fixes, and live production validation).
