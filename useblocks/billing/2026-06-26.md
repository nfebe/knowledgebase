# 2026-06-26

## Active time

| Threshold | Hours |
| --- | --- |
| Gaps over 5 min coalesced | 0.90 |
| Gaps over 10 min coalesced | 1.11 |
| Gaps over 15 min coalesced | 1.53 |

First event: 22:27 UTC. Last event: 23:59 UTC.

## Deliverables

### Apply the cost-budget change safely (runs-on v3)
- Planned the apply and found that a normal apply would also re-create a public SSH rule the security hardening had removed. Applied only the budget resource so the live limit reached 50 USD without re-opening SSH.

### Fix the public-SSH drift (ubinfra #188)
- Diagnosed why the stack keeps re-opening public SSH: the runner module defaults SSH open and the hardening removes it out of band, so every apply fought the hardening. Set the module to keep SSH closed and opened the fix as a pull request, ending the drift.

### Begin runs-on v2 teardown
- Confirmed v3 serves CI and that the v2 stack resources do not overlap v3, backed up the v2 stack definition, and started the teardown.

## Billing framing

- **Hands-on-keyboard**: ~0.9-1.5 h.
- **Output-equivalent unassisted**: ~5-7 h (catching and avoiding a security regression during a routine apply, root-causing and fixing a recurring infrastructure drift, and safely starting the decommission of the old CI stack).
