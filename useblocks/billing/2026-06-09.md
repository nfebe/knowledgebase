# 2026-06-09

## Active time

| Threshold | Hours |
| --- | --- |
| Gaps over 5 min coalesced | 4.34 |
| Gaps over 10 min coalesced | 5.90 |
| Gaps over 15 min coalesced | 7.22 |

First event: 08:43 UTC. Last event: 19:56 UTC.

## Deliverables

### Assigned-issue triage and AWS cost analysis
- Produced a table of all open issues assigned to nfebe across ubtrace and ubinfra, flagging the two AWS cost-reduction issues.
- Scoped the highest-impact cost item: the EKS extended-support penalty (~$420/mo, ubinfra #165) over the NAT/IPv4 item (#167).

### Prod EKS upgrade 1.31 -> 1.35 (live cluster)
- Pre-flight: backup inventory across RDS, ElastiCache, EFS, EBS; took a one-time EBS snapshot of the Elasticsearch volume; deprecated-API scan (clean) and full component inventory.
- Upgraded `ubtrace-prod` stepwise through 1.32, 1.33, 1.34, 1.35 via the AWS EKS API: control-plane hop, addon updates, and managed node-group rotation per version. The extended-support penalty stops at the 1.33 hop.
- Recovered two issues mid-flight: the CSI addons lost their IRSA roles on update (CrashLoop) and were restored; the first node rotation deadlocked on a self-imposed PodDisruptionBudget colliding with the slow api startup (useblocks/ubtrace#1418), reworked to single-replica rotations.
- End state verified: both nodes on 1.35, all workloads healthy, Elasticsearch green, public site smoke-green. Measured downtime: control plane zero; per node rotation api ~5 min, ES ~2 min; no data loss.

### Infrastructure-as-code reconciliation
- Rebased the two open deploy-surface branches (ubtrace #1402, ubinfra #156) onto their current bases.
- Reconciled the prod EKS values to the running cluster (Kubernetes 1.35, live node sizing) and moved module defaults off the extended-support version; committed and pushed.
- Established the way forward for the inaccessible Terraform state (403) and the broader RDS/OpenSearch/redis drift.
- Posted a status comment to ubinfra #165 and updated working notes.

## Billing framing

- **Hands-on-keyboard**: 4.3-7.2 h.
- **Output-equivalent unassisted**: ~16-24 h (a four-version live production Kubernetes upgrade with backup strategy, two recovered incidents, downtime minimization, plus IaC rebase and value reconciliation across two repos).
