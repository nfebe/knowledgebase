# 2026-06-18

## Active time

| Threshold | Hours |
| --- | --- |
| Gaps over 5 min coalesced | 1.27 |
| Gaps over 10 min coalesced | 1.50 |
| Gaps over 15 min coalesced | 1.93 |

First event: 09:00 UTC. Last event: 21:49 UTC.

Note: this figure covers only the useblocks working sessions. Most of the day went to non-useblocks clients, which are billed separately and excluded here.

## Deliverables

### RunsOn v2 to v3 upgrade: triage and planning (ubinfra #180)
- Verified a reported ubCode CI outage attributed to RunsOn and surveyed the existing footprint: the v2 stack was console-created, with Terraform covering only peripheral concerns (queue and table alarms, VPC flow logs, compliance, security-group and network-ACL hardening, and an AMI rebuild utility), not the runner stack itself.
- Opened #180 as the parent task for moving RunsOn onto reproducible infrastructure and planned the cutover approach.

## Billing framing

- **Hands-on-keyboard**: ~1.3-1.9 h (useblocks portion only).
- **Output-equivalent unassisted**: ~3-5 h (verifying a CI outage against live infrastructure, auditing what was and was not under Terraform, and scoping a control-plane migration).
