# 2026-06-23

## Active time

| Threshold | Hours |
| --- | --- |
| Gaps over 5 min coalesced | 1.00 |
| Gaps over 10 min coalesced | 1.11 |
| Gaps over 15 min coalesced | 1.29 |

First event: 13:00 UTC. Last event: 22:00 UTC.

## Deliverables

### Merge the EKS deploy surface (ubtrace #1402, ubinfra #156)
- Landed both PRs once the review items were addressed: the ubTrace Helm chart and Terraform modules, and the ubinfra prod overlay, deploy wrapper, and runbooks.

### Pin the prod helm dry-run and deploy to the published chart (ubinfra #185)
- Switched the dry-run and deploy from cloning a moving branch to the pinned, published OCI chart, so previews are reproducible and actually render the Keycloak policy fields the overlay sets (previously dropped silently). Opened the change as a pull request.
- Fixed a related gap where the CI deploy capped the helm timeout below the chart's cold-start budget, which could roll back a healthy pod.

## Billing framing

- **Hands-on-keyboard**: ~1.0-1.3 h.
- **Output-equivalent unassisted**: ~4-6 h (merging two coupled infrastructure PRs in the right order, then closing the reproducibility and false-green gap in the deploy preview).
