# 2026-05-25

## Active time

| Threshold | Hours |
| --- | --- |
| Gaps over 5 min coalesced | 3.33 |
| Gaps over 10 min coalesced | 4.43 |
| Gaps over 15 min coalesced | 5.31 |

First event: 10:18 UTC. Last event: 20:28 UTC.

## Deliverables

### Prod deploy: ubTrace v1.4.0-dev.39
- Helm release `ubtrace` ended at "failed rev 29" (helm record); live cluster brought to dev.39 across all 5 ubTrace components via `kubectl patch` + `kubectl set image`.
- Brief outage (~21 min) during investigation when the OLD pre-existing api pod was deleted to test singleton-resource contention.
- Recovery: kubectl-patched api / worker `startupProbe.failureThreshold` to give a 15-minute boot budget; rolled images forward; service restored.

### Investigation
- Confirmed `ub-backend` image at dev.39 is byte-identical to dev.38 (same `main.js` sha256); only worker scheduler service changed between releases (PR #1417 ASPICE work).
- Root cause of new pods hanging at startup: `UbtSphinxParserService.onModuleInit` walks `/data/input_build` recursively scanning for `ubtrace_project.toml` files; over the last 3 days the worker indexed many more project versions; cold NFS metadata walk now exceeds the chart's 150-second startup probe budget; kubelet SIGKILLs before `app.listen()`.
- Live cluster has drift between rendered Deployments (kubectl-patched probe budgets) and the helm release record.
- Issue [useblocks/ubtrace#1418](https://github.com/useblocks/ubtrace/issues/1418) opened against the root cause.

## Billing framing

- **Hands-on-keyboard**: 3.3-5.3 h.
- **Output-equivalent unassisted**: ~10-14 h (live-prod recovery during user-facing outage, root-cause investigation against a misleading first hypothesis, deploy of dev.39).
