# ubTrace PROD Deployment Checklist — team.useblocks.com (FILLED)

Filled copy of ubinfra #159, based on the 1.5.0 deploy (2026-06-03) and the follow-on selector incident (2026-06-05/06). Honest status: items we actually exercised are ticked; UI features we did not click are left unchecked and marked "not exercised."

- **Release / tag:** `1.5.0` (image `ghcr.io/useblocks/ub-backend:1.5.0`). 1.5.0-dev.0 went out 2026-05-29; stable 1.5.0 on 2026-06-03.
- **Previous version:** `1.4.0` (stable) / `1.5.0-dev.0`
- **Operator:** nfebe
- **Date:** 2026-06-03 (deploy); 2026-06-05/06 (incident)
- **Deploy start (UTC):** ~22:54
- **Duration (min):** ~7 (helm rev 33 -> 34, 6m38s)

---

## 1. Infrastructure deploy

- [x] **Terraform apply successful?** n.a. — helm-only deploy (values overlay bump + #1446 ingress annotation). No Terraform change this release. Prod has no live TF state to apply against (access is via `aws ec2 describe-instances` + SSM, not `tofu output`).
- [x] **Helm upgrade successful?** Yes. `helm upgrade ubtrace ... -f values-team.yaml --atomic`. Revision **33 -> 34**, 6m38s.
- [x] **DB migration successful?** Yes (inferred sound): the migrate hook runs inside the `--atomic` upgrade; rev 34 is live, so the upgrade (and its migrate job) did not roll back. Not separately tailed.

### Manual steps needed (candidates to automate)
- `aws sso login --profile ubtrace-prod-admin` then SSM bastion tunnel to the private EKS API (scripted in deploy.sh `tunnel`, but login is manual).
- Added the #1446 ALB `idle_timeout.timeout_seconds=300` annotation to `values-team.yaml` by hand. Prod deploys from `values-team.yaml`, not `values-eks.yaml` where the upstream fix landed, so the annotation had to be ported.

---

## 2. Cluster health

- [x] **All pods Up & Ready?** Yes. All 5 components on 1.5.0, 1/1 Running at deploy.
- [x] **No CrashLoopBackOff / ImagePullBackOff / Pending.** Correct.
- [ ] **API readiness NOT flapping** — FAILED later. On 2026-06-05/06 readiness flapped to `search: degraded "Elasticsearch unreachable"`. Not an ES fault: the single api event loop was stalled inside the selector EFS walk, so the ES `pingFresh` (2000ms) timed out. Filed #1477.
- [ ] **Worker NOT in ingestion loop** — FAILED. The loop is present (#1419/#1423): `ubtrace:worker:queue` at ~246k, draining ~165/10h. Scheduler re-enqueues `ubconnect` (`manifest=none, delta=new`) and 9 outdated-NDJSON versions every ~30s. Cleared the backlog manually 2026-06-06 (rename+backup).

### 2a. Pod restarts before healthy
- [x] **ubtrace-api** — restarts: 0
- [x] **ubtrace-worker** — restarts: 0
- [x] **ubtrace-frontend** — restarts: 0
- [x] **ubtrace-admin** — restarts: 0
- [x] **ubtrace-builder** — restarts: 0
- [x] **ubtrace-keycloak** — restarts: 0
- [x] **ubtrace-elasticsearch** — restarts: 0 (up 10+ days)

### 2b. Log warnings/errors from pod start
- **ubtrace-worker** — recurring `Queue backpressure: 246k`, `ASPICE config change detected: ubconnect (manifest=none)`, `Found 9 version(s) with outdated NDJSON format`. The ingestion-loop signature.
- **ubtrace-api** — no ES/error lines at LOG level at deploy; later, selector requests entered the handler with no response logged (hang).
- Other pods — no notable start-time errors observed.

---

## 3. UI smoke test — https://team.useblocks.com

- [x] **Site opens fast** — at deploy, yes. Later DEGRADED: docs/selector hung during the incident.
- [x] **Auth works / logout works** — auth redirect 302, app 200 at smoke. No logout 500 observed.
- [x] **Admin page works** — admin host 200.
- [ ] **Load orga / project / version fast** — FAILED (the incident). Selector returned no options; `GET /artifacts/organizations` hung (7 called, 0 returned). Root cause: 149s synchronous full-EFS scan rebuild with no single-flight, aggravated by the worker loop. #1477. (Related to the Redis-thrash slowness theme in #1381.)
- [ ] **Docs app works** (images, navigation, links, back/forward, view/edit-source, diagrams) — not exercised feature-by-feature this session.
- [ ] **Docs toctree search** — not exercised.
- [ ] **'Search needs'** — not exercised.
- [ ] **Version listed bottom-left matches release** — not verified.
- [ ] **Dashboards (SN-Demo, trends)** — not exercised.
- [ ] **Schema validation** — not exercised.
- [ ] **Orphan detection** — not exercised.
- [ ] **ASPICE Readiness (SN-Demo)** — not exercised.
- [ ] **Traceability Tree** — not exercised.
- [ ] **Activity Feed (ingest-loop check)** — loop confirmed present via worker logs/queue, not via the feed UI.
- [ ] **Non-admin visibility** — not exercised.

> Note: the deploy smoke focused on reachability (HTTP status of the 4 hosts + readiness) plus the #1446 patch verification. Per-feature UI clicks were not part of this session; the selector incident then consumed the follow-up.

---

## 4. New features quick-check

- [ ] CHANGELOG / release notes for 1.5.0 — not reviewed this session.
- Features verified: none feature-by-feature.
- Questions for the team: none raised.

---

## 5. Result summary

- [x] Deployment **GREEN with notes** at deploy time (helm rev 34, all pods healthy, 4 hosts + readiness OK, #1446 patch verified live), then **DEGRADED** on 2026-06-05/06 due to the selector/worker-loop issue (mitigated, not yet code-fixed).
- Bugs filed: #1477 (selector hang/empty). Patched during deploy: #1446 (ingest 504 — scratch-dir extraction + ALB idle_timeout=300). Worked: #1423 (worker queue manual unblock).
- Questions raised: none for the PO this round.

---

## 6. Bug reporting

- #1477 filed to the #1429 quality bar: summary, confirmed root cause, live measurements (149s/3,299 round trips, 7-called/0-returned, Redis state), affected files, proposed fix.
- Evidence captured before mitigating (per the #1429 lesson): walk timing, queue sample, ES health, readiness polls, all recorded before the worker pause / api restart / queue clear.
- Bugs found this deploy: #1477 (selector). Pre-existing confirmed live: #1423 / #1419 (worker loop), #1418 (api startup EFS scan).
